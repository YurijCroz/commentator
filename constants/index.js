"use strict";

const CONSTANTS = Object.freeze({
  COMMENTS_AND_TOTAL_PAGE_CACHE: "commentsAndTotalPage",
  DEFAULT_SORT_BY: "createdAt",
  DEFAULT_SORT_DIRECT: "DESC",
});

module.exports = CONSTANTS;
